@using HospitalManagement.Models.EntityModels
@using HospitalManagement.Models.ViewModels
@model IEnumerable<Patient>




@{
    PageHeader ph = new PageHeader() { Title = "List of Doctor" };
}
<partial name="../Shared/Widgets/_PageHeader.cshtml" model="ph" />



<div class="card">
    <div class="card-header">
        <h3 class="card-title">
            <a asp-controller="Patient" class="btn btn-success" asp-action="Create">Add Patient</a>
        </h3>

        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                <i class="fas fa-minus"></i>
            </button>
            <button type="button" class="btn btn-tool" data-card-widget="remove" title="Remove">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
    <div class="card-body">
        

        <div class="card">
            
            <!-- /.card-header -->
            <div class="card-body p-0">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th style="width: 10px">ID</th>
                            <th>Patient Name</th>
                            <th>Mobile </th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (Patient patient in Model)

                        {
                            <tr>
                                <td>@patient.Id</td>
                                <td>@patient.Name</td>
                                <td>@patient.Mobile</td>
                                <td>
                                    <div class="btn-group">
                                        <a asp-controller="Patient" asp-action="Details" asp-route-id="@patient.Id" class="btn btn-success btn-sm">
                                            <i class="far fa-file-alt"></i>
                                        </a>
                                        <a asp-controller="Patient" asp-action="Edit" asp-route-id="@patient.Id" class="btn btn-warning btn-sm">
                                            <i class="fas fa-pencil-alt"></i>
                                        </a>
                                        <a asp-controller="Patient" asp-action="Delete" asp-route-id="@patient.Id" class="btn btn-danger btn-sm">
                                            <i class="fas fa-trash-alt"></i>
                                        </a>
                                    </div>
                                </td>
                                
                            </tr>

                            
                        }

                        
                        
                    </tbody>
                </table>
                <partial name="../Shared/Widgets/_Pagination.cshtml" />


            </div>
            <!-- /.card-body -->
        </div>


    </div>
    <!-- /.card-body -->
    <div class="card-footer">
        Footer
    </div>
    <!-- /.card-footer-->
</div>


